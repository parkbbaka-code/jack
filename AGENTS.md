# IROORI — Phase 1 (소원나무)

전체 사양은 docs/SPEC.md. UI는 docs/DESIGN.md. 법적 페이지는 docs/LEGAL.md.

## 지금 만들지 않는 것
개인 나무 · 물주기 · growth.ts · 복귀 화면 · 리듬 히트맵 · 저널
결제/Toss 연동 · 푸시 알림 · PWA 설치 프롬프트 · 하단 탭바
(legacy/ 디렉터리는 건드리지 않는다)

## 절대 틀리면 안 되는 것
1. wishpapers 생성은 서버 전용. 클라이언트에 열면 무료 소원지가 무한 생성된다
2. slot 좌표는 생성 시 확정해 문서에 저장. 매 렌더 랜덤이면 남의 소원이 자리를 옮긴다
3. 만료를 delete로 처리하지 않는다. expiresAt 경과분은 쿼리에서 제외하고 개수만 집계
4. /s/[shareId]에 noindex. 남의 소원이 검색에 뜨면 프라이버시 사고다
5. 오브젝트 위에 사용자 텍스트를 렌더하지 않는다. 세로쓰기도 쓰지 않는다
6. 소원나무 1회 방문 = 최근 20장 + stats 문서 1건 = 21 reads 고정.
   전체 소원지를 로드하지 않는다
